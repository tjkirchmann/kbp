"""Discord bot: a long-lived gateway connection that listens for inbound events
(slash commands, messages) and posts outbound. Runs as its own process
(`python -m app.bot`), separate from the API and the Procrastinate worker."""
